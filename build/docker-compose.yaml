services:
  context:
    image: redis:7.0.9
    ports:
      - 6379:6379
  bot:
    image: str1kez/ocrgpt_bot
    build: .
    env_file:
      - ../.env
    # volumes:
    #   - "../config:/app/config"
